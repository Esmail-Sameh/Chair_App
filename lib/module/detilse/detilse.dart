import 'package:flutter/material.dart';

class DetilseScreen extends StatelessWidget {
  static String routName = '/detilseScreen';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}